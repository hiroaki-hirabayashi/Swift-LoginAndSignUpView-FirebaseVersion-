# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Swift-LoginAndSignUpView-FirebaseVersion-' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Swift-LoginAndSignUpView-FirebaseVersion-

  pod 'Firebase/Analytics'

  target 'Swift-LoginAndSignUpView-FirebaseVersion-Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Swift-LoginAndSignUpView-FirebaseVersion-UITests' do
    # Pods for testing
  end

end
